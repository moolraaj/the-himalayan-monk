/** @type {import('next').NextConfig} */
const nextConfig = {
   
    images:{
        unoptimized:true
    },
    // basePath:'/docx',
    trailingSlash:true,

   
    
};

export default nextConfig;
