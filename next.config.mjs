/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath:'/docx',
    // output:'export',
    images:{
        unoptimized:true
    },
    trailingSlash:true,
};

export default nextConfig;
